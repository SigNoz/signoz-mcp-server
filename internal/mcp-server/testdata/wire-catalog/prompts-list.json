{
  "method": "prompts/list",
  "protocolVersion": "2025-11-25",
  "request": {
    "id": 0,
    "jsonrpc": "2.0",
    "method": "prompts/list",
    "params": {}
  },
  "httpStatus": 200,
  "contentType": "application/json",
  "framing": "json",
  "response": {
    "id": 0,
    "jsonrpc": "2.0",
    "result": {
      "prompts": [
        {
          "arguments": [
            {
              "description": "Name of the metric to compare",
              "name": "metricName",
              "required": true
            },
            {
              "description": "First time period (e.g., '24h ago to 12h ago')",
              "name": "period1",
              "required": true
            },
            {
              "description": "Second time period (e.g., 'last 12h')",
              "name": "period2",
              "required": true
            }
          ],
          "description": "Compare a metric across two time periods to identify changes or regressions.",
          "name": "compare_metrics"
        },
        {
          "arguments": [
            {
              "description": "Service name to investigate",
              "name": "service",
              "required": true
            },
            {
              "description": "Time range to search (e.g., '1h', '6h', '24h'). Defaults to '1h'.",
              "name": "timeRange"
            }
          ],
          "description": "Search error logs, aggregate error traces, and list top operations for a service.",
          "name": "debug_service_errors"
        },
        {
          "arguments": [
            {
              "description": "Alert rule ID to triage",
              "name": "alertId",
              "required": true
            }
          ],
          "description": "Triage an active alert by fetching its details, history, related logs, and traces.",
          "name": "incident_triage"
        },
        {
          "arguments": [
            {
              "description": "Service name to analyze",
              "name": "service",
              "required": true
            },
            {
              "description": "Time range to analyze (e.g., '1h', '6h', '24h'). Defaults to '1h'.",
              "name": "timeRange"
            }
          ],
          "description": "Query latency metrics, aggregate trace durations, and identify slow operations for a service.",
          "name": "latency_analysis"
        }
      ]
    }
  }
}
